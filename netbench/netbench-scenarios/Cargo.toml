[package]
name = "netbench-scenarios"
version = "0.1.0"
authors = ["AWS s2n"]
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
clap = { version = "2", features = ["color", "suggestions"] }
humantime = "2"
netbench = { path = "../netbench" }
