[package]
name = "s2n-quic-rustls"
version = "0.51.0"
description = "Internal crate used by s2n-quic"
repository = "https://github.com/aws/s2n-quic"
authors = ["AWS s2n"]
edition = "2021"
rust-version = "1.71"
license = "Apache-2.0"
# Exclude corpus files when publishing to crates.io
exclude = ["corpus.tar.gz"]

[features]
fips = ["s2n-quic-crypto/fips", "rustls/fips"]

[dependencies]
bytes = { version = "1", default-features = false }
# By [default](https://docs.rs/crate/rustls/latest/features) rustls includes the `tls12` feature.
rustls = { version = "0.23", default-features = false, features=["std", "aws-lc-rs", "logging"] }
rustls-pemfile = "2"
s2n-codec = { workspace = true, default-features = false, features = ["alloc"] }
s2n-quic-core = { workspace = true, default-features = false, features = ["alloc"] }
s2n-quic-crypto = { workspace = true, default-features = false }

[dev-dependencies]
insta = { version = "1", features = ["json"] }
s2n-quic-core = { workspace = true, features = ["testing"] }
