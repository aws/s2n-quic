[package]
name = "s2n-quic-transport"
version = "0.51.0"
description = "Internal crate used by s2n-quic"
repository = "https://github.com/aws/s2n-quic"
authors = ["AWS s2n"]
edition = "2021"
rust-version = "1.71"
license = "Apache-2.0"
# Exclude corpus files when publishing to crates.io
exclude = ["corpus.tar.gz"]

[features]
default = ["std"]
std = ["futures-channel/std"]
unstable_resumption = []
unstable-provider-dc = []

[dependencies]
bytes = { version = "1", default-features = false }
futures-channel = { version = "0.3", default-features = false, features = ["alloc"] }
futures-core = { version = "0.3", default-features = false, features = ["alloc"] }
hashbrown = "0.15"
intrusive-collections = "0.9"
once_cell = "1"
s2n-codec = { workspace = true, features = ["bytes"] }
s2n-quic-core = { workspace = true, features = ["alloc"] }
siphasher = "1.0"
smallvec = { version = "1", default-features = false }

[dev-dependencies]
bolero.workspace = true
futures-test = "0.3" # For testing Waker interactions
insta = { version = "1", features = ["json"] }
s2n-codec = { workspace = true, features = ["testing"] }
s2n-quic-core = { workspace = true, features = ["testing"] }

[lints.rust.unexpected_cfgs]
level = "warn"
check-cfg = [
    'cfg(s2n_quic_dump_on_panic)',
    'cfg(feature, values("testing"))',
]
